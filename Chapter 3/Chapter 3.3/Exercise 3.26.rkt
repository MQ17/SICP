#lang sicp

;compare key to current
;if current key is key, then return value
;if smaller, then move to left subtree
;if bigger, move to right subtree
;if no more subtrees, then return false
;too lazy to write an actual implementation