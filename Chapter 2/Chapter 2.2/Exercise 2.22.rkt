#lang racket

; same issue as what I had for 2.20. if you think of the list as a stack,
; he first pushes the first items into answer first, meaning that the last items
; get pushed on last, meaning the list is reversed

;In the second case, the pointer has to be the second element in the pair
